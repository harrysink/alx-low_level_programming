#include <stdio.h>

/**
 *main - should print "Programming is like building a multilingual puzzle"
 *Return: 0 if successsful, if not, print non-zero
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
