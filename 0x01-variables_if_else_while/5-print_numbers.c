#include <stdio.h>

/**
 * main - this function prints all singhle digit numbers of
 * base 10, starting from 0, followed by a new line
 * Return: 0 always when successful
 */
int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
	{
		printf("%d\n", b);
	}
	return (0);
}
