#include "main.h"

/**
 * _isupper - this program checks for upercase
 * characters
 * @c: character for checking
 * Return: always 1 is c is uppercase, else 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
